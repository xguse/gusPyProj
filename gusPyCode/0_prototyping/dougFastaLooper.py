#def advanceLines(fileObj,currentLine=None):
    ## basically advance until you find a '>' then you can tell it to save the fasta name if you want.
    ## (for now i am assuming a single fasta entry per file, you can fix this yourself if you want multiples)
    
    #if currentLine:
        #line = 

#def loopFastaByLineWithOverlap(fastaFilePath):
    
    ## open file but dont read anything
    #fasta = open(fastaFilePath,'rU')
    
    
    
    
    #line     = fasta.readline().strip('\n')
    #nextLine = fasta.readline().strip('\n')
    
    #while 1:
        #line = line + nextLine[0:15]
        
        ## # #
        ## insert whatever search and result logging code you want here
        ##
        ##    your code here
        ## # #
        
        ## we now check if the file is done
        ##if not 
        
        
        #line     = nextLine
        #nextLine = fasta.readline().strip('\n')
        
        
        
#if __name__ == '__main__':
    #f = '/Users/biggus/Desktop/testLooper.fas'
    
    #loopFastaByLineWithOverlap(f)
    
    
try:
    1==3
except:
    print 'Nope'